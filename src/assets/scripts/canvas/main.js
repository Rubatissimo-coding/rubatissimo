export function initCanvas(canvas, keys) {
  ctx = canvas.getContext("2d")
  canvas.height = window.innerHeight
  canvas.width = window.innerWidth

  let tile,
    particle,
    tiles = [],
    tileX,
    tileWidth = window.innerWidth / 88,
    keyNumber

  class Tile {}
}
